; Sublimity

	; Messes with org mode, need to autoload maybe
	;(require 'minimap)

; Settings

	;(setq minimap-window-location 'right)
	;(define-key evil-normal-state-map (kbd ",map") 'minimap-toggle)

(provide 'user-init-minimap)
