;;; package --- Summary: Setup avy
;;; Commentary:

(require 'avy)
(require 'ace-jump-buffer)

;;; Code:

(setq avy-keys '(
                    ?t ?n ?s ?e ?f ?u ?d ?h
                    ))

(setq avy-all-windows 'all-frames)

(provide 'init-avy)

;;; init-avy ends here
