; Avy

	(require 'avy)
    (require 'ace-jump-buffer)

; Settings

    (setq avy-keys '(
                     ?t ?n ?s ?e ?f ?u ?d ?h
                     ))

    (setq avy-all-windows 'all-frames)

(provide 'init-avy)
