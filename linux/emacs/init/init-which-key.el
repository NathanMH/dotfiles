;;; package --- Summary: Setup which key
;;; Commentary:

(require 'which-key)

;;; Code:

(setq which-key-allow-evil-operators t)
(setq which-key-show-operator-state-maps t)

(provide 'init-which-key)

;;; init-which-key ends here
